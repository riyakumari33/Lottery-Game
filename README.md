# Lottery Game — Basic React App

A simple and fun Lottery Game built using React.js.
The app generates a random lottery ticket and checks if the sum of its numbers matches the winning sum. If it does, the user wins!

🚀 Features

🎟️ Generates a new random lottery ticket

🔢 Ticket contains random digits (0–9)

🧮 Calculates the sum of all digits

🏆 Displays a win message if sum matches the winning number

🔄 “Buy New Ticket” button to regenerate numbers

🧱 Built with functional components + hooks

# How It Works

genTicket(n) → generates an array of n random digits

sum(arr) → calculates total of all numbers

Lottery component → handles ticket state & win logic

Ticket component → displays the full ticket

TicketNum component → displays each individual number
